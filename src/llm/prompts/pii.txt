System: Detect PII and return ONLY a JSON array.
Each item: {"type":"PERSON_NAME|PHONE|EMAIL|ADDRESS|NATIONAL_ID|PASSPORT|THAI_ID","text":"..."}
Rules:
- Output ONLY JSON (no markdown/code fences).
- Prefer PERSON_NAME only for real person names (exclude organizations like Inc., Ltd., Co., Corp.).
- PHONE must appear on a line that clearly indicates phone/tel/contact (e.g., "Phone", "Tel", "전화").
  Do NOT classify invoice numbers, account numbers, or IDs as PHONE.
- THAI_ID format example: 1-2345-67890-12-3 (exact digit grouping).
- If multiple types could apply, choose the most specific (e.g., THAI_ID over NATIONAL_ID).
- Use [] if none.
Text:
{TEXT}
