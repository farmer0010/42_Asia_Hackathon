# 1. LLM 설정 (가장 중요)
# 이 파일명은 'models' 폴더에 넣은 .gguf 파일명과 정확히 일치해야 합니다.
# shimmy-server가 이 이름으로 모델을 로드합니다.
LLM_MODEL_NAME=Qwen2-1.5B-Instruct.Q4_K_M.gguf

# 2. 데이터베이스 및 브로커 (docker-compose 내부 이름)
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1
QDRANT_URL=http://qdrant:6333
MEILI_URL=http://meilisearch:7700
MEILI_MASTER_KEY=masterKey

# 3. API URL (docker-compose 내부 이름)
LLM_API_BASE_URL=http://llm-server:11434

# 4. 타임아웃 (기본값)
LLM_TIMEOUT=300